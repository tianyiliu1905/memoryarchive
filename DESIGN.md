# Memory Archive — 技术设计

一个「冥想盆」式的作品集。项目不是列表里的条目，而是漂浮在盆中的光斑；
浏览的动作不是点击链接，而是俯身、聚焦、潜入。

本文记录**为什么这么写**，而不是**写了什么**——后者读代码更快。

---

## 目录

- [1. 整体结构](#1-整体结构)
- [2. 核心抽象：深度状态机](#2-核心抽象深度状态机)
- [3. 三维场景](#3-三维场景)
- [4. 交互：手势如何变成意图](#4-交互手势如何变成意图)
- [5. 项目详情：横向日记](#5-项目详情横向日记)
- [6. 性能与加载策略](#6-性能与加载策略)
- [7. 关键参数速查](#7-关键参数速查)
- [8. 扩展指南](#8-扩展指南)
- [9. 已知取舍](#9-已知取舍)

---

## 1. 整体结构

```
src/
├── data/          数据层：项目内容、分类定义、类型
├── store/         全局状态（zustand）：深度状态机
├── three/         三维场景：盆、光斑、相机、命中区、着色器
├── components/    二维覆盖层：标题、索引、转场、光标
├── journal/       项目详情：横向日记（懒加载）
├── hooks/         指针、手势、环境探测
└── styles/        全局样式
```

分层的核心原则：**三维负责氛围，二维负责信息**。

文字永远不进入 WebGL——它由 DOM 渲染，因此可选中、可被搜索引擎读取、
可被读屏软件朗读。三维只负责那些 DOM 做不到的事：光的弥散、
粒子的漂浮、景深的推拉。

两层通过 `store/archive.ts` 的深度状态同步，互不直接引用。

---

## 2. 核心抽象：深度状态机

整个应用只有一个真正的全局状态——**当前处在哪一层深度**。

```
basin  ──(滚轮蓄力 / 点击光斑群)──>  cluster
cluster ──(滚轮蓄力 / 点击光斑)──>  diving ──>  project
project ──(上滚 / 返回)──>  surfacing ──>  cluster ──>  basin
```

定义在 `src/store/archive.ts`：

```ts
export type Depth = 'basin' | 'cluster' | 'diving' | 'project' | 'surfacing';
```

### 为什么需要 diving 和 surfacing 这两个「中间态」

它们看起来是多余的——直接 `cluster → project` 不就行了？

不行。转场动画需要一段**明确归属的时间**。如果没有 `diving`，
那么在动画播放的 900ms 里，应用要么已经是 `project`（于是日记提前出现，
穿帮），要么还是 `cluster`（于是无法判断该不该禁用手势）。

把转场本身作为一个状态，意味着：

- `BasinOverlay` 可以据此隐藏首页 UI（`hidden = depth === 'diving' || 'project'`）
- `useDiveGesture` 可以据此忽略滚轮，避免转场中被打断
- `MoteCluster` 可以据此把光斑透明度压到 0.35，让位给转场色块

`surfacing` 同理，它保证退场动画能完整播完再卸载组件。

### 冷却锁

跃迁后有 `COOLDOWN = 900ms` 的锁（`useDiveGesture.ts`）。
触控板的惯性滚动会在手指离开后继续派发几十个 wheel 事件，
没有这把锁，一次轻扫就会直接从盆底穿到项目详情。

---

## 3. 三维场景

### 3.1 光斑布局的单一来源

`src/three/layout.ts` 是所有位置计算的唯一出处。

这一点至关重要：光斑由 GPU 着色器绘制，而点击检测由 CPU 的射线检测完成。
两者是**完全独立的两套计算**。一旦它们对「光斑在哪」的理解出现分歧，
就会出现「看得见却点不到」的鬼畜体验。

所以 `moteOffsets()` 同时被两处消费：

- `MoteCluster.tsx` → 写入 geometry 属性，交给顶点着色器
- `HitZones.tsx` → 放置不可见的球形命中代理

同样地，`focusedOffset()` 的数学必须与 `motesMaterial.ts`
顶点着色器第 4 步**逐字对应**。改一处必须改另一处。

### 3.2 为什么用黄金角分布

```ts
const GOLDEN = 2.399963;
const ang = i * GOLDEN + rnd() * 0.5;
const radial = MOTE_SPREAD * (0.62 + 0.38 * Math.sqrt((i + 0.6) / count));
```

均匀随机分布在小样本（3~5 个）下经常会「结块」——两颗光斑挨得极近，
视觉上糊成一坨，也让点击变得困难。

黄金角是低差异序列，任意数量都自然散开。
`sqrt()` 让半径随索引增长而放缓，避免外圈过于稀疏。
叠加的 `rnd()` 扰动打破完美对称，让它看起来像自然漂浮而非机械排列。

随机数用固定种子（由 `clusterId` 推导），因此布局是**确定性**的——
刷新页面光斑不会跳位。

### 3.3 着色器里的 uniform 全部走插值

`MoteCluster.tsx` 的 `useFrame` 里，没有任何一个 uniform 是直接赋值的：

```ts
focusV.current += (targetFocus - focusV.current) * d * 2.1;
u.uFocus.value = focusV.current;
```

这是踩过坑的结果。`uDive` 最初是直接赋值的，而 `useDiveGesture`
在跃迁瞬间会把 `chargeRef` 硬切为 0。结果就是所有光斑「被吸向中心」
的位移在一帧内消失——肉眼看到的是猛地弹一下再回位。

改成指数衰减后，归零过程被摊到几帧里，弹跳变成了自然回弹。

**规律：任何由离散事件驱动的连续视觉量，都必须插值。**

`delta` 也必须夹紧（`Math.min(delta, 0.05)`）。标签页切回前台时
浏览器会给出一个巨大的 delta，不夹的话插值会直接冲过头。

### 3.4 盆不跟着鼠标转

早期版本里相机位置和注视点同时跟随指针。结果是视线方向绕着盆心摆动，
观感就是「整个盆在跟着鼠标旋转」——非常晕。

现在只平移相机，`lookAt` 锁死（`CameraRig.tsx`）：

```
之所以不再改 lookAt：相机位置与注视点同时跟随指针时，
视线方向会绕着盆心摆动，观感就是「整个盆跟着鼠标转」。
纯平移只产生轻微的视差位移，空间感还在，但盆是静止的。
```

配套地，盆面的高光从「弧形」改成了完整的环（`basinMaterial.ts`），
因为弧形高光本身也会暗示一个旋转方向。

### 3.5 涟漪只提亮，不压暗

`basinMaterial.ts` 里水波用 `exp()` 而非 `sin()`：

```glsl
float waves = exp(-pd * pd * 4.0) * uPointerForce * 0.016 * basin;
```

`sin()` 的负半周会把盆面压暗，在浅色背景上表现为**脏灰色的波纹**，
像是渲染错误。`exp()` 恒为正，只做加法提亮，符合「光在液面上扩散」
的物理直觉。

### 3.6 光斑的径向渐变

为了精确复刻 CSS `radial-gradient` 的观感，着色器里用分段线性插值
还原色标（`motesMaterial.ts`）：

```glsl
if (r < 0.25)      a = mix(s0, s1, r / 0.25);
else if (r < 0.60) a = mix(s1, s2, (r - 0.25) / 0.35);
else               a = mix(s2, 0.0, (r - 0.60) / 0.40);
```

比起 `smoothstep`，分段插值能精确对齐设计稿给定的停靠点。
代价是渐变尾巴更长，所以基础 `aSize` 从 5.2 下调到 3.0，
否则相邻光斑的外晕会叠在一起。

---

## 4. 交互：手势如何变成意图

`src/hooks/useDiveGesture.ts` 把所有输入统一翻译成两个标量：

- `chargeRef`：纵向下潜蓄力，-1 ~ 1
- `panRef`：横向平移蓄力，-1 ~ 1

### 蓄力而非直接触发

滚轮不是「滚一下换一层」，而是累积到 1.0 才跃迁，且不满时持续回落
（`DECAY = 0.955`）。

这带来两个好处：**物理上**避免误触，**叙事上**制造阻尼感——
你必须有意识地持续用力才能潜下去，这正好呼应「潜入记忆」的隐喻。

`BasinOverlay` 订阅这个值并绘制进度环，让蓄力过程可见。

### 主轴锁定

斜向拖拽会同时产生 dx 和 dy。若不处理，一次手势可能既切换分类又下潜一层。

```ts
if (ax > AXIS_LOCK || ay > AXIS_LOCK) {
  axis = ax > ay * 1.15 && s.depth === 'cluster' ? 'x' : 'y';
}
```

主轴一旦判定就锁定到手势结束。`1.15` 的偏置让纵向略占优势——
下潜是主线动作，横向浏览是支线。

### dragGuard

鼠标拖拽切换分类时，`pointerup` 之后浏览器仍会派发 `click`。
若不拦截，松手的位置正好在某颗光斑上就会误触发潜入。

`dragGuardRef` 把「本次交互是否构成拖拽」暴露给 `HitZones`，
后者在 `onClick` 开头检查并提前返回。

### 可达性

方向键 / PageUp / PageDown / Escape 均可完成全部导航。
`useEnvironment` 探测 `prefers-reduced-motion`，命中时降级动效。

---

## 5. 项目详情：横向日记

`src/journal/` 把纵向滚轮映射为横向位移，模拟翻阅长卷。

- **惯性插值**（`FRICTION = 0.082`）而非直接跟手，保持「缓慢、有重量」的基调
- **分层视差**：背景最慢、图片略慢、注释略快，制造纸张叠层的深度
- **过度滚动退出**：滚到最左端后继续反向滚动超过 `EXIT_THRESHOLD = 150px`
  即浮出水面，无需寻找关闭按钮

### 配图是程序化生成的

`Plate.tsx` 根据 `seed` 和 `variant` 用 Canvas 画抽象图形，
不加载任何外部图片。

这是个刻意的取舍：作品集的截图往往会过期、失效、或涉及保密内容。
抽象图形永不失效，体积为零，且与整体的「记忆是模糊的」基调一致。

需要真实截图时，见[第 8 节](#8-扩展指南)的处理建议。

---

## 6. 性能与加载策略

### 6.1 真正的瓶颈不是内容

一个常见的担心是「项目多了会不会加载变慢」。实测数据（gzip 后）：

| 构成 | 体积 | 占比 |
|---|---|---|
| 依赖（three / react / framer-motion） | 300 KB | **91.7%** |
| 业务代码 + 全部 12 个项目文案 | 27 KB | 8.3% |

单个项目的边际成本是实测出来的：把项目数从 12 砍到 6 重新构建，
业务块从 26.9 KB 降到 21.9 KB。

**即 6 个完整项目 ≈ 5 KB gzip，单个约 0.8 KB。**

再加 100 个项目也只增加约 80 KB——不到 three.js 的 4/10。
文字不是瓶颈，依赖才是。

### 6.2 拆包：按变更频率切分

`vite.config.ts` 的 `manualChunks` 不按功能切，而是按**变更频率**切：

| 块 | 体积 (gzip) | 变更频率 |
|---|---|---|
| `three` | 217 KB | 几乎不变 |
| `react` | 47 KB | 几乎不变 |
| `motion` | 38 KB | 几乎不变 |
| `index`（业务） | 27 KB | 频繁 |

好处是改一行业务代码只让 27 KB 的块失效，用户缓存里的 300 KB 依赖
完全不用重下。如果打成单个大包，任何改动都会让全部缓存作废。

### 6.3 懒加载与预取

`MemoryJournal` 只在进入项目时才需要，所以从主包里摘出来：

```tsx
const MemoryJournal = lazy(() => import('@/journal/MemoryJournal')...);
```

**但只写 `lazy()` 是不够的。** 如果组件无条件挂载在 `<Suspense>` 里，
React 首次渲染就会立刻触发加载——懒加载形同虚设。
（这是实测发现的：首屏仍然请求了 MemoryJournal.js。）

必须在挂载层面就加条件：

```tsx
const journalMounted =
  depth === 'diving' || depth === 'project' || depth === 'surfacing';

{journalMounted && <Suspense fallback={null}><MemoryJournal /></Suspense>}
```

注意必须包含 `surfacing`，否则退场动画会被截断。

同时在用户聚焦到某个光斑群时**预取**——此刻他很可能接着潜入，
提前在后台拉取，真正点击时已经就绪：

```tsx
useEffect(() => {
  if (depth === 'cluster') import('@/journal/MemoryJournal');
}, [depth]);
```

`Suspense` 的 fallback 是 `null` 而非加载指示器：转场动画本身
已经覆盖了这段等待，再叠一个 spinner 反而会打断叙事。

### 6.4 实测结果

Fast 3G（1.6 Mbps / 150ms RTT），3 次平均：

| 指标 | 优化前 | 优化后 |
|---|---|---|
| First Paint | 3224 ms | **440 ms** |
| FCP | 2943 ms | 1985 ms |
| 首屏 JS | 1.1 MB | 328 KB |

First Paint 提升约 7 倍。主要来自两点：HTML 不再等待单个巨包，
以及 `modulepreload` 让依赖块并行下载。

---

## 7. 关键参数速查

改动这些数值前，先读对应文件里的注释——大多有踩坑记录。

### 布局 `three/layout.ts`

| 常量 | 值 | 作用 |
|---|---|---|
| `BASIN_RADIUS` | 4.2 | 盆的世界半径 |
| `MOTE_SPREAD` | 0.58 | 光斑在群内的分布半径 |
| `FOCUS_SCALE` | 1.85 | 聚焦时的外扩倍数 |
| `FOCUS_K` | 0.55 | 外扩的非线性指数 |

> `FOCUS_*` 系列必须与 `motesMaterial.ts` 顶点着色器同步修改。

### 手势 `hooks/useDiveGesture.ts`

| 常量 | 值 | 作用 |
|---|---|---|
| `CHARGE_PER_WHEEL` | 0.0072 | 每像素 deltaY 的蓄力贡献 |
| `DECAY` | 0.955 | 蓄力每帧回落系数 |
| `COOLDOWN` | 900 | 跃迁后的冷却，ms |
| `AXIS_LOCK` | 9 | 主轴判定阈值，px |

### 日记 `journal/MemoryJournal.tsx`

| 常量 | 值 | 作用 |
|---|---|---|
| `FRICTION` | 0.082 | 横向插值系数，越小越重 |
| `EXIT_THRESHOLD` | 150 | 过度滚动退出阈值，px |

---

## 8. 扩展指南

### 添加一个项目

只需编辑 `src/data/projects.ts`，追加一个 `Project` 对象。

布局、命中区、索引列表、章节排版全部自动适配——
`moteOffsets()` 会根据数量重新计算黄金角分布。

唯一需要留意的是**每群的数量**。当前是 4/5/3。
超过 7~8 颗时，光斑在盆全景下会开始显得拥挤，
此时应调大 `MOTE_SPREAD` 或考虑分页。

### 添加一个分类

1. 在 `data/types.ts` 的 `ClusterId` 加上新 id
2. 在 `data/clusters.ts` 补齐配置（颜色、anchor、文案）
3. 在 `store/archive.ts` 的 `CLUSTER_SEQUENCE` 里排好顺序

`anchor` 是盆平面上的归一化极坐标，注意与已有分类保持距离。

### 如果将来要用真实图片

程序化配图的好处见[第 5 节](#5-项目详情横向日记)。若确实需要真实截图，
建议：

- 用 `loading="lazy"` + `<picture>` 提供 AVIF/WebP
- 按章节懒加载，而非进入项目就全量拉取
- 图片是唯一可能真正拖垮这个项目的资源类型——文字不会

### 如果项目数量增长到 50+

届时才需要考虑：

- 把 `projects.ts` 拆成按分类的独立模块，随日记一起懒加载
- 首屏只保留光斑布局所需的元数据（id / cluster / scale），
  完整文案按需拉取

但在此之前不必优化——见 6.1 的数据，这不是瓶颈。

---

## 9. 已知取舍

**项目数据无法完全懒加载。** 首屏的光斑布局和命中区需要知道
每个分类有多少项目、各自的 `scale` 和 `archiveId`，
所以 `data/projects.ts` 目前仍在主包里。

好在代价可控：源文件 38.9 KB，但打包压缩后对首屏的实际贡献约 10 KB。
真要优化，应拆成「布局元数据」和「章节内容」两个模块，
前者留主包，后者随日记走。

**three.js 无法再瘦身。** 217 KB (gzip) 已经是 tree-shaking 后的结果。
唯一的进一步优化是换成手写 WebGL，但会失去 R3F 的声明式心智模型，
对这个体量的项目不划算。

**没有服务端渲染。** 首屏必须等 JS 执行完才有内容。
对作品集而言可以接受——它本来就是个需要交互才有意义的东西。
若要 SEO，可以考虑预渲染一份纯文字的静态版本作为 `<noscript>` 回退。
